import numpy as np

if __name__ == "__main__":
    # arr = np.array([2,5,6,3,4])
    # sorted_arr = np.sort(arr)
    # print(sorted_arr)

    # user_id = {9,34,21,12}
    # user_dict = dict(zip(user_id, range(len(user_id))))
    # print(user_dict)

    user_id = {1,2,3}
    ids = np.array(user_id)
    print(ids)
    print(type(ids))
